#include <stdio.h>
/**
 * main - Entry point
 * @argc: number of array argv element
 * @argv: array of pointer to char
 * Return: Always (0)
 */
int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
